CREATE TABLE consultas (
                         id serial PRIMARY KEY,
                         idMedico INTEGER,
                         idPaciente INTEGER,
                         data VARCHAR(30)
);
